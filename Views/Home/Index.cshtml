@{
    ViewBag.Title = "Welcome";
}

@model IEnumerable<string>

<div class="container">
    <h2>Voila the top 5 restaurants of Provo</h2>
    <div class="list-group">
        @foreach (string i in Model)
        {
            <p>@i</p>
        }
    </div>
</div>


